[package]
name = "lp-glsl-diagnostics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared GLSL diagnostics: error codes, GlslError, source locations (for new compiler stack)"

[lints]
workspace = true

[dependencies]

[features]
default = []
# Reserved for dependents that want to align feature flags; no extra deps yet.
std = []
