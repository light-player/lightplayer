# Review - feature/static-shapes

## Target

- **Branch/PR:** feature/static-shapes
- **Base:** feature/radio (`127f14fd`)
- **Head:** `3b653a8e`
- **Review date:** 2026-05-21
- **Reviewer:** Codex

## Summary

Reviewed the rebased static-shapes branch with emphasis on the static catalog,
runtime-state registration split, shape sync/paging, project-read streaming, and
server tick path. The main memory-directed structure looks coherent: authored
static shapes resolve through `SlotShapeLookup`, runtime state registration has
its own named path, and the old generated all-shapes bootstrap helpers are gone.

## Findings

| Issue | Severity | Status | Summary |
| --- | --- | --- | --- |
| [01-unlimited-read-project-shapes-is-paged.md](01-unlimited-read-project-shapes-is-paged.md) | P2 | fixed | Non-streaming `Engine::read_project` silently paged `limit: None` shape reads at 64 entries while streaming treated the same request as complete. |

## Validation

- `cargo fmt --check`: passed before this review pass.
- `git diff --check`: passed before this review pass.
- `cargo check -p lpc-engine`: passed before this review pass.
- `cargo test -p lpc-model`: passed before this review pass.
- `cargo test -p lpc-engine`: passed before this review pass.
- `cargo check -p fw-emu --target riscv32imac-unknown-none-elf --profile release-emu`: passed before this review pass.
- `cargo check -p fw-esp32 --target riscv32imac-unknown-none-elf --profile release-esp32 --features esp32c6,server`: passed before this review pass.
- Review spot check: `cargo test -p lpc-model slot_shape_registry::tests::static_catalog_pages_without_registering_all_static_shapes -- --nocapture` passed.

## Notes

- I did not find a current resident-memory regression in the runtime-state split.
- The remaining `to_owned_shape()` sites still look like the already-recorded
  followup 03 rather than a blocker for this branch.
- The review artifact itself is intentionally uncommitted.
