#!/bin/bash
# Removed with `lps-q32-metrics-app` (Stage VII). Recover the script and app from git history if needed.
echo "lps-q32-metrics-app was deleted; use lpvm-cranelift-based tooling or restore from git." >&2
exit 1
