[package]
name = "lpc-slot-mockup"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
lpc-model = { path = "../lpc-model", features = ["std", "derive"] }
lpc-view = { path = "../lpc-view", features = ["std"] }
lpc-wire = { path = "../lpc-wire", features = ["std"] }
toml = { workspace = true, features = ["display"] }

[build-dependencies]
lpc-slot-codegen = { path = "../lpc-slot-codegen" }

[lints]
workspace = true
