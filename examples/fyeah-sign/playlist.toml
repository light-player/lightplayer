kind = "Playlist"
idle_entry = 1
default_fade = 0.35

[bindings.time]
source = "bus#time.seconds"

[entries.1]
name = "idle"
fade_after = 0.12
node = { def = { path = "./idle.toml" } }

[entries.2]
name = "blast"
duration = 6.0
fade_after = 2.0
node = { def = { path = "./blast.toml" } }

[entries.2.bindings.trigger]
source = "bus#trigger"
