kind = "Playlist"
idle_entry = 1
default_fade = 0.35

[bindings.time]
source = "bus#time.seconds"

[entries.1]
name = "idle"
fade_after = 0.12
node = { ref = "./idle.toml" }

[entries.2]
name = "active"
duration = 4.0
fade_after = 0.8
node = { ref = "./active.toml" }

[entries.2.bindings.trigger]
source = "bus#trigger"
