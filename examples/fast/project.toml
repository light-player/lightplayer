kind = "Project"
name = "fast"

[nodes.output]
def = { path = "./output.toml" }

[nodes.clock]
def = { path = "./clock.toml" }

[nodes.texture]
def = { path = "./texture.toml" }

[nodes.shader]
def = { path = "./shader.toml" }

[nodes.fixture]
def = { path = "./fixture.toml" }
