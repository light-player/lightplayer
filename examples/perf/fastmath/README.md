Frozen baseline for `lp-cli profile` regression tests.

This tree is a copy of the basic example project so the basic example can evolve without breaking profile snapshots. (Previously lived at `examples/mem-profile/`.)
