# Phase 1: Write design doc

## Scope

Create `docs/design/q32.md` — the single source of truth for Q32 semantics.

## Status

**Done** — written during plan design iteration. The doc covers:
encoding, conversions, saturating arithmetic, div-by-zero, constants,
GLSL builtins, relational builtins, `@unsupported` policy, comparisons,
backend conformance, and reference implementation pointers.

## Validate

Read through `docs/design/q32.md` and confirm it matches the decisions
recorded in `00-notes.md`.
