*.bk
*.pyc
*.swo
*.swp
*.swx
*~
.*.rustfmt
.mypy_cache
\#*\#
.idea
.vscode/*
!.vscode/settings.json
!.vscode/keybindings.json
.vs/
rusty-tags.*
tags
target
.z3-trace
foo
/.cargo
publish
vendor
# The committed CodeMirror bundle is a build artifact by philosophy but a
# tracked file by design (offline builds, no npm): un-ignore it.
!lp-app/lpa-studio-web/public/vendor/
!lp-app/lpa-studio-web/public/vendor/**
# Per-worktree harness launch config: generated by `just claude-launch-json`
# with this worktree's hashed dev port. A committed copy pins one worktree's
# port on every other worktree (docs/adr/2026-07-27-worktree-local-launch-json.md).
/.claude/launch.json
*.coredump
# rustc internal-compiler-error dumps land in the CWD (repo root) on a crash.
rustc-ice-*.txt
*.smt2
*.cwasm
/artifacts
.ci-artifacts
testcase*.wat
testcase*.wasm
testcase*.dna
testcase*.json
perf.data*
.cursor
.DS_Store
.builtins-source-hash
traces/
profiles/
lp-app/lpa-studio-web/public/fw-browser-worker.js
# The Tailwind bundle dx compiles from lpa-studio-web/tailwind.css on every
# build. UNLIKE the CodeMirror bundle above, this one is regenerated rather
# than vendored, so tracking it is actively harmful: git merges a generated
# bundle by text, and the merge of two branches' bundles is neither branch's
# — utilities that exist only on one side silently vanish. lpa-studio-web's
# build.rs writes a placeholder when it is absent, so plain `cargo check`
# still resolves `asset!()` on a fresh clone.
lp-app/lpa-studio-web/assets/tailwind.css
lp-app/lpa-studio-web/dist/
lp-app/lpa-studio-web/story-images/.new/
lp-app/lpa-studio-web/story-images/.scratch/

# lp-xt fixture build artifacts (regenerable via lp-xt/fixtures/build.sh, esp toolchain)
lp-xt/fixtures/elf/
lp-xt/fixtures/reloc/obj/
lp-xt/fixtures/fp/obj/
