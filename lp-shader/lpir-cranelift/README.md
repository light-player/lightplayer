# lpir-cranelift

**LPIR → Cranelift → machine code** (RISC-V JIT on device; host JIT / object with `std`). Optional **`glsl`** feature pulls in `lp-glsl-naga` for string entrypoints. See [`../CRATES.md`](../CRATES.md).
