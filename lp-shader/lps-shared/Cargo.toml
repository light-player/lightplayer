[package]
name = "lps-shared"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "GLSL semantic types shared across compilers (new stack)"

[lints]
workspace = true

[features]
default = []
std = []

[dependencies]
lps-q32 = { path = "../lps-q32" }