# lp-glsl-filetests-app

CLI binary to **run GLSL filetests** (filters, targets); depends on `lp-glsl-filetests`. Prefer `scripts/glsl-filetests.sh` for CI parity. See [`../CRATES.md`](../CRATES.md).
